# PID-SAR-3++ SSL Results Index

This page is the SSL document map. The SSL writeup was split so the primary benchmark claims can be read independently from tuning sweeps, diagnostics, and appendix ablations.

Use:

- `docs/PID_SAR3_SSL_MAIN_RESULTS.md` for the corrected protocol, headline comparisons, and primary pair->target downstream benchmark.
- `docs/PID_SAR3_SSL_APPENDIX_ABLATIONS.md` for hyperparameter sweeps, subset diagnostics, supplementary `y_*` proxy analyses, and secondary ablations.
- `docs/PID_SAR3_REPRO_COMMANDS.md` for dataset-generation commands (dataset note figures).
